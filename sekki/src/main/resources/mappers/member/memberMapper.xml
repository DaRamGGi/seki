<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.care.sekki.member.MemberMapper">
	<resultMap type="com.care.sekki.member.MemberDTO" id="memberDto">
		<id column="id" property="id"/>
		<result column="pw" property="pw"/>
		<result column="user_name" property="userName"/>
		<result column="email" property="email"/>
		<result column="address" property="address"/>
		<result column="mobile" property="mobile"/>
		<result column="profilePictureUrl" property="profilePictureUrl"/>
		
	</resultMap>
	<select id="loginProc" resultMap="memberDto">
		SELECT * FROM sekki_member2 WHERE id=#{id}
	</select>
	
	<insert id="registerProc" parameterType="com.care.sekki.member.MemberDTO">
		INSERT INTO sekki_member2 VALUES(#{id}, #{pw}, #{userName}, #{address},
		 #{mobile}, #{profilePictureUrl}, #{height}, #{weight})
	</insert>
	
	<select id="count">
		<choose>
	  		<when test="select == 'id'">
	  			SELECT count(id) FROM sekki_member2 WHERE id like '%'||#{search}||'%'
	  		</when>
	  		<when test="select == 'mobile'">
	  			SELECT count(id) FROM sekki_member2 WHERE mobile like '%'||#{search}||'%'
	  		</when>
	  		<otherwise>
	  			SELECT count(id) FROM sekki_member2
	  		</otherwise>
		</choose>
	</select>
  
	<select id="memberInfo" resultMap="memberDto">
		<choose>
			<when test="select == 'id'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, id, pw, user_name, address, mobile 
				FROM sekki_member2 WHERE id like '%'||#{search}||'%'
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<when test="select == 'mobile'">
				SELECT AAA.* FROM 
				(SELECT rownum as rn, id, pw, user_name, address, mobile 
				FROM sekki_member2 WHERE mobile like '%'||#{search}||'%' 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</when>
			<otherwise>
				SELECT AAA.* FROM 
				(SELECT rownum as rn, id, pw, user_name, address, mobile  
				FROM sekki_member2 )AAA 
				WHERE AAA.rn BETWEEN #{begin} AND #{end}
			</otherwise>
		</choose>
	</select>
	
	<update id="updateProc" parameterType="com.care.sekki.member.MemberDTO">
		UPDATE sekki_member2
		SET pw=#{pw}, user_name=#{userName}, address=#{address}, mobile=#{mobile} 
		WHERE id=#{id}
	</update>
	
	<delete id="delete">
		DELETE FROM sekki_member2 WHERE id=#{id}
	</delete>
</mapper>







  